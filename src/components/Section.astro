---
interface Props {
  title?: string
  class?: string
}

const { title, class: className } = Astro.props
---

<section class:list={['py-20', className]}>
  {title && <h2 class="mb-10 text-3xl font-bold">{title}</h2>}
  <slot />
</section>
